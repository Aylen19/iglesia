import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistembo.sandy',
  appName: 'sandy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
